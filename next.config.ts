

/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["cdn.dummyjson.com", "dummyjson.com"],
        // Добавляем нужный домен
    },
};

module.exports = nextConfig;
